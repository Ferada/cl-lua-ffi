cl-lua-ffi - FFI bindings to the Lua API.

This is not [cl-lua][], an implementation of the Lua language in Common Lisp,
but provides access to the embedding API instead.

[cl-lua]: https://github.com/AyeGill/cl-lua
